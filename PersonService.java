package com.example.SpringLDAPExample.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.SpringLDAPExample.entity.Person;
import com.example.SpringLDAPExample.repository.PeopleRepository;
//import static org.springframework.ldap.query.LdapQueryBuilder.query;

public class PersonService {
	
	/*
	 * @Autowired private PeopleRepository personRepository;
	 * 
	 * public List<Person> getAllPersons(){ //List<Person> personNames =
	 * ldapTemplate.search(query().where("objectClass").is("person"), new
	 * PersonAttributesMapper()); return personRepository.getAllPersons(); }
	 */
	
	

}
